# 毕业设计（论文）综述

## 1. 综述

### 1.1 题目背景

随着物联网、智能穿戴设备及工业自动化技术的飞速发展，高精度、实时的姿态测量与监测系统在无人机导航、机器人控制、虚拟现实、人体运动分析等领域的需求日益增长。姿态测量系统的核心在于稳定、准确地获取物体的三维空间方位（即俯仰角 Pitch、横滚角 Roll、航向角 Yaw），并实现数据的可视化呈现与高效管理。传统姿态测量系统多依赖于单一平台（如 PC 端或嵌入式独立设备），存在跨平台兼容性差、数据共享不便、用户交互体验单一等问题。近年来，便携式可穿戴姿态监测设备（如智能手环、运动分析手持终端）已成为研究热点，但多数系统仍局限于本地显示或单一终端，难以实现远程、随时随地多端查看。为此，本设计提出一套手持便携式姿态测量终端（基于 ESP32-C3 + MPU6050 + 可充电锂电池），结合云端实时数据库，实现数据无线上传与 QT 桌面、移动 APP、Web 等多终端同步监测，具有重要的理论价值与工程应用前景。

### 1.2 研究意义

本研究旨在设计并开发一套"采集-传输-处理-显示-管理"全流程的姿态测量系统，其意义主要体现在以下几个方面：

#### 1.2.1 技术集成与创新

将嵌入式传感器技术、无线通信技术、姿态解算算法、桌面应用开发与移动端开发相结合，完成一个功能完整的软硬件集成系统，是对学生多学科知识综合运用能力的重要锻炼。尤其是基于 HTML5 + Three.js + WebSocket 的 Web 可视化层与 Qt WebEngine 的深度集成，体现了"一套前端代码、多端复用"的现代软件架构设计理念。

#### 1.2.2 解决实际问题

系统采用手持便携设计（锂电池供电、TP4056 安全充电），实现姿态数据的实时采集与低功耗运行；通过云端同步（自建 Node.js WebSocket 服务器），支持远程、多终端（桌面 + 移动 + Web）随时查看与管理，为实验室科研、教学演示、以及需要姿态反馈的各类控制场景提供一套低成本、高可用的解决方案。

#### 1.2.3 方法论价值

通过对四元数解算、传感器数据融合、WebSocket 网络通信协议、跨平台软件架构（Qt WebEngine 复用 Web 前端）等关键技术的深入研究与实践，所形成的手持终端 + 云端协同方案可为同类系统的开发提供参考。

#### 1.2.4 符合专业培养目标

本课题紧密围绕测控技术与仪器专业的核心——测量、控制与系统集成，全面涵盖了传感器应用、信号处理、软件设计等关键环节，契合专业人才培养要求。

### 1.3 国内外相关研究情况

国内外在姿态测量领域的研究已相当深入，主要集中在传感器技术、解算算法和系统应用三个层面。

#### 1.3.1 在传感器与解算算法方面

MPU6050 等 MEMS 惯性传感器因其体积小、成本低被广泛应用。姿态解算的核心是从陀螺仪和加速度计的原始数据中融合出稳定姿态。经典方法包括基于四元数的互补滤波[1]和卡尔曼滤波[2]。Madgwick 等人提出的梯度下降算法在开源社区广为流传[3]。国内学者也提出了多种改进算法以应对传感器噪声和漂移[4,5]。这些研究为本课题终端算法的实现奠定了坚实基础。

#### 1.3.2 在系统设计与实现方面

早期系统多采用微控制器（如 STM32）采集数据，通过串口在 PC 端上位机显示[6]。随着无线技术发展，基于 Wi-Fi 或蓝牙的传输方案成为主流[7]。上位机软件多采用 LabVIEW、C# 或 Qt 开发，实现数据可视化[8,9]。近年来，移动端（Android/iOS）APP 用于实时显示姿态数据也成为研究热点[10,11]。随着 Web 技术的成熟，基于 HTML5 + Three.js + WebSocket 的浏览器端实时可视化方案也逐渐兴起，与 Qt WebEngine 结合后可在桌面、移动端实现跨平台部署，无需为每个平台分别开发可视化逻辑。这些研究展示了多终端显示的技术可行性。

#### 1.3.3 在研究趋势与不足方面

当前研究呈现三大趋势：一是算法优化，追求在嵌入式设备上实现更高精度、更低功耗的解算[12]；二是系统云端化、网络化，实现数据的远程存储与分析[13]；三是向可穿戴/手持便携方向发展，如基于 ESP32+MPU6050 的姿态手环、智能手套或跌倒检测设备。然而，多数现有系统功能相对单一，或仅关注桌面端，或仅关注移动端，将手持便携终端（锂电池供电）与统一 Web 可视化前端（HTML5 + Three.js）通过 Qt WebEngine 深度融合，构建"一套前端代码同时服务于浏览器、Qt 桌面与移动端"的完整系统，这方面的公开研究和完整案例相对较少。此外，系统级的长时间稳定性测试与精度定量分析在相关文献中也较少详细涉及。

综上所述，本研究拟在现有技术基础上，进行系统性集成，重点解决多终端协同、跨平台数据流统一管理以及系统级工程实现与测试等问题。

---

## 2. 本课题研究的主要内容和拟采用的研究方案、研究方法或措施

### 2.1 主要研究内容

#### 2.1.1 手持便携终端硬件设计与低功耗优化

选用 ESP32-C3 + MPU6050 搭建集成化手持终端；集成 3.7V 可充电锂电池与 TP4056 充电保护模块，实现安全充电与便携供电。

#### 2.1.2 姿态测量与高精度解算算法研究

研究基于 MPU6050 传感器的原始数据采集与预处理方法；重点研究并实现在 ESP32-C3 嵌入式终端上运行的四元数姿态解算算法，以及互补滤波或卡尔曼滤波数据融合算法，以获取稳定、高精度的姿态角。

#### 2.1.3 云端实时通信机制设计与实现

设计一套轻量级、可扩展的自定义应用层通信协议；基于 WebSocket 的自定义 JSON 协议，实现 ESP32-C3 终端、云端与多终端之间的稳定、双向数据传输，确保数据的实时性与可靠性。

#### 2.1.4 跨平台软件系统开发

1. **Web 可视化前端开发**：使用 HTML5 + JavaScript + Three.js 开发浏览器端实时可视化页面，通过 WebSocket 接收四元数数据并驱动 Three.js 3D 模型实时旋转，同时显示 Roll/Pitch/Yaw 数值及端到端链路延迟。该前端为整个应用层的核心可视化组件，被所有终端复用。

2. **QT 桌面管理软件开发**：利用 Qt 框架与 `QWebEngineView` 开发轻量级桌面 Web 查看器，将上述 Web 可视化页面嵌入 Qt 原生窗口，实现跨平台桌面运行。Qt 的工作重点为：`QWebEngineView` 集成与生命周期管理、服务器地址配置界面、系统托盘与通知、可选后台线程负责心跳检测或日志记录。通过复用已有 Web 前端，避免重复开发可视化逻辑，显著降低维护成本。

3. **移动端轻量级 APP 开发**：使用 Qt Creator 以 C++ 开发 Android 平台应用程序，通过 Qt WebView 加载相同的 Web 可视化页面，实现姿态数据的实时展示，与桌面端保持一致的可视化体验。

4. **Web 端**：浏览器直接访问 Node.js 服务器提供的静态页面，无需安装任何客户端，即可随时查看姿态数据。

#### 2.1.5 系统集成与综合测试

将硬件终端、桌面软件、移动 APP、Web 端进行系统集成；制定测试方案，对系统的功能完整性、测量精度、实时性、网络稳定性以及长时间（≥60 小时）运行的可靠性进行综合测试与验证。

### 2.2 研究方案与措施

#### 2.2.1 系统总体方案

采用"感知-解算-低功耗上传-云端同步-多端应用"的分层架构：

- **感知与解算层**：集成的 ESP32-C3 + MPU6050 + 锂电池手持终端，负责数据采集与本地四元数解算。
- **传输层**：基于 ESP32-C3 内置 Wi-Fi 与 WebSocket 协议，实现数据上传至云端 Node.js WebSocket 服务器。
- **应用层**（三端统一复用同一套 Web 前端）：
  - **Web 端**：浏览器直接访问，零安装成本。
  - **Qt 桌面端**：`QWebEngineView` 嵌入 Web 可视化页面，与浏览器端共用同一前端代码，Qt 负责原生窗口与桌面集成。
  - **移动端 APP**：Qt WebView 加载 Web 可视化页面，与桌面端保持统一体验。

这种"一套 Web 前端、多端复用"的架构设计，消除了重复开发可视化逻辑的冗余，保证了三端可视化效果的一致性，同时充分利用 Qt 的跨平台能力实现原生桌面/移动端集成。

#### 2.2.2 硬件平台构建

选用 ESP32-C3 开发板，MPU6050 作为姿态传感器，集成 TP4056 模块与锂电池（1000mAh LiPo），设计便携手持外壳（3D 打印或现成）。该方案利用 ESP32-C3 的内置 Wi-Fi 功能，简化硬件设计，提高系统可靠性。

#### 2.2.3 软件设计与开发

1. **嵌入式终端软件**：使用 ESP-IDF 开发环境，采用 C/C++ 语言编程。实现 I2C 驱动读取 MPU6050 数据，植入并优化四元数姿态解算算法，通过 ESP32-C3 的 Wi-Fi 模块以 WebSocket 模式上传数据（JSON 格式，含四元数 q0/q1/q2/q3 及单向延迟估计 lat_es）至自建 Node.js 服务器。

2. **Node.js 云端服务器**：基于 Node.js + `ws` 库实现 WebSocket 服务器，区分 `/esp` 路径（ESP32 数据上传）与默认路径（浏览器/桌面客户端订阅），将 ESP32 上传的四元数数据实时广播给所有已连接的应用层客户端，并附加 `serverTime` 字段用于链路延迟计算。

3. **Web 可视化前端**：使用 HTML5 + JavaScript + Three.js 开发。通过 WebSocket 接收四元数数据，直接驱动 Three.js 中的 3D 立方体旋转（附坐标轴、方向锥体），同时在浏览器端将四元数转换为欧拉角（Roll/Pitch/Yaw）用于数值显示，并实时计算 ESP→Server、Server→Client 及总链路延迟。

4. **QT 桌面软件**：使用 C++ 和 Qt Creator 开发。以 `QWebEngineView` 为核心，加载 Web 可视化页面。Qt 主线程负责 WebEngine 生命周期管理与窗口布局，可选后台线程处理心跳检测或运行日志记录。提供服务器地址配置、系统托盘常驻等原生桌面功能。

5. **移动端 APP**：使用 Qt Creator，采用 C++ 语言开发 Android 应用。以 Qt WebView 加载相同的 Web 可视化页面，利用 Node.js 服务器进行 WebSocket 通信，实现与桌面端相同的实时姿态可视化体验。

6. **通信协议设计**：JSON 格式的 WebSocket 消息。主要消息类型：
   - `{ type: "quat", q0, q1, q2, q3, lat_es, serverTime }`（ESP32→服务器→客户端）
   - `{ type: "euler", roll, pitch, yaw, lat_es, serverTime }`（兼容旧版欧拉角上报）
   - `{ type: "ping", tick }` / `{ type: "pong", tick }`（RTT 测量）

#### 2.2.4 测试与验证方法

1. **功能测试**：黑盒测试各模块功能，验证三端（Web/桌面/移动）均能正确接收并显示姿态数据。
2. **性能测试**：使用高精度转台作为参考，对比测量系统输出，计算姿态角误差（如均方根误差 RMSE）；使用网络调试工具测试数据传输延迟与丢包率；增加续航测试（≥4-8 小时）、充电功能验证、云端总链路延迟（<300ms）验证。
3. **稳定性测试**：搭建测试环境，使系统连续运行 60 小时以上，记录运行状态与数据。

---

## 3. 本课题研究的重点及难点，前期已开展工作

### 3.1 重点

1. **高精度姿态解算算法的嵌入式实现**：在 ESP32-C3 资源与功耗约束下，优化算法代码，平衡计算精度与实时性。

2. **稳定可靠的多终端通信机制**：设计稳定的 WebSocket JSON 通信协议，处理网络断开重连等问题，保证桌面端与移动端数据同步。

3. **Web 可视化前端设计**：Three.js 四元数直驱 3D 模型旋转（含 IMU 坐标系到 Three.js 世界坐标系的轴变换）、实时多级延迟计算（ESP→Server→Client）、响应式布局适配桌面与移动端不同屏幕尺寸。

4. **Qt WebEngine 集成**：`QWebEngineView` 与本地/远程 WebSocket 服务的协同工作，Qt 桌面应用的跨平台打包与分发（Windows/Linux/macOS）。

### 3.2 难点

1. **传感器误差补偿与融合算法调优**：MPU6050 的零偏、温漂等误差对精度影响大，如何有效补偿并与陀螺仪数据进行最优融合是难点。

2. **系统实时性保障**：从数据采集到终端解算，再到网络传输与多端显示，整个链路延迟需控制在可接受范围（<200ms）。

3. **跨平台软件的一致性与协同**：确保 Qt 桌面端、移动端与 Web 浏览器端在不同操作系统下，对同一数据源的可视化表现和交互逻辑保持一致。

4. **Web 前端与 WebSocket 的协同调试**：浏览器端四元数→欧拉角转换精度需与嵌入式端保持一致；Three.js 世界坐标系与 MPU6050 传感器坐标系（IMU: x-前, y-左, z-上）之间的轴变换逻辑需经过实测校准，确保 3D 模型旋转方向与实物一致。

### 3.3 前期已开展工作

1. **文献调研与方案论证**：已完成相关中外文献的搜集与阅读，明确了技术路线，完成了系统的总体方案设计。

2. **硬件平台选型与搭建**：已完成 ESP32-C3 开发板、MPU6050 传感器模块等关键硬件的选型与采购，并成功搭建了硬件电路。

3. **基础驱动与测试**：已在开发环境中搭建 ESP32-C3 项目，初步实现了对 MPU6050 的 I2C 驱动读取，并成功测试了 ESP32-C3 内置 Wi-Fi 的 WebSocket 客户端连接功能。

4. **云端服务器与 Web 前端实现**：已完成 Node.js WebSocket 服务器（区分 `/esp` 与浏览器路径、四元数广播、RTT 测量支持）与 Web 可视化前端（Three.js 四元数驱动 3D 渲染、多级延迟实时显示、响应式布局）的开发与联调，ESP32 → 服务器 → 浏览器的完整数据链路已验证可用。
